A simple dotnet solution to do .NET benchmarks using  [BenchmarkDotNet](https://github.com/dotnet/BenchmarkDotNet)

# Run benchmark
Simply run the following command in the solution directory.

```cs
dotnet run -c Release
```